{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd B && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd B && uvicorn server:app --host 0.0.0.0 --port $PORT"
  }
}
